##### Commit 2

**Question 1b:** What does this commit do?

```R
demo <- function(n) {
    result <- list(
        sum = n[["x"]] + n[["y"]]
    )
    return(result)
}
```

```Python
def demo(n):
    answer = {
        "sum": n["x"] + n["y"]
    }
    return answer
```

